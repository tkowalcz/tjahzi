# Tjahzi allocates its off heap log buffer and starts the log shipper thread
# when the logging system starts. Neither direct buffers nor running threads
# can be captured in the image heap, so all Tjahzi classes must initialize
# at run time even when the surrounding logging framework is initialized at
# build time.
Args = --initialize-at-run-time=pl.tkowalcz.tjahzi
